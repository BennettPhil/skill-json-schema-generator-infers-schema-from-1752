# json-schema-generator-infers-schema-from-1752

A JSON Schema generator that infers a schema from sample JSON data. Given one or more JSON examples, produc...

## Quick Start

```bash
./scripts/run.sh demo-target
```

## Prerequisites

- Bash 3.2+ (macOS compatible)
